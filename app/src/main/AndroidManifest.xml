<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.NovaHome"
        android:supportsRtl="true">

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:theme="@style/Theme.NovaHome.Home"
            android:configChanges="orientation|screenSize|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Gaming Hub activity -->
        <activity
            android:name=".gaming.GamingHubActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaHome.Gaming"
            android:label="Gaming Hub" />

        <!-- Hide Apps Settings -->
        <activity
            android:name=".ui.HideAppsActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaHome"
            android:label="Hidden Apps" />

        <!-- Launcher Settings -->
        <activity
            android:name=".ui.LauncherSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.NovaHome"
            android:label="Home Settings" />

    </application>
</manifest>
